<html>
    <head>
        <meta charset="utf-8" />
        <link rel="stylesheet" type="text/css" href="./assets/css/bootstrap.min.css"/>
    </head>
    <body>
        <table>
            <thead>
                <th></th>
            </thead>
            <tbody>
                <% for(i=0; i < users.length; i++){ %>
                <tr>
                    <td><%=users[i].name %></td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </body>
</html>